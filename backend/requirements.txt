# Extra dependencies for the settings service (backend/).
#
# Kept separate from the project's own requirements.txt so a headless install
# that only runs the agent from the command line does not need a web server.
#
# Install alongside the main requirements:
#     pip install -r requirements.txt -r backend/requirements.txt

fastapi>=0.115,<1.0          # the HTTP surface
uvicorn[standard]>=0.30      # the server that runs it
cryptography>=42.0           # Fernet, for the encrypted secrets store
ruamel.yaml>=0.18            # YAML round-trip, so saving keeps the comments

# httpx and gspread are already in the project's requirements.txt: httpx makes
# the provider test calls, gspread verifies a Google Sheets connection by
# actually opening the sheet.
