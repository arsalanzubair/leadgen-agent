# secrets and local state -- never commit
.env
.secrets/
# The encrypted per-workspace credential store written by the settings service,
# and the profile/targeting edits made through the dashboard. Encrypted is not
# the same as safe to publish.
config/tenants/*/
!config/tenants/*.yaml
.data/
data/imports/
*.sqlite
*.sqlite3
*.db

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
